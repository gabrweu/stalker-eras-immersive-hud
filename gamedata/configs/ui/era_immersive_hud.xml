<?xml version="1.0" encoding="windows-1251" ?>
<window>
	<era_lock x="8" y="726" width="6" height="8" stretch="1">
		<texture r="210" g="210" b="210" a="160">ui_era_ihud_lock</texture>
	</era_lock>

	<!-- Radiation infection bar: we draw ONLY the progress fill (era_rad_fill). The background
	     groove (slot + trefoil) is the BHS/Minimalist HUD panel's own baked art -- we no longer
	     override it, so BHS panel retextures show through. Canvas coords mirror SidHUD's
	     rad_health so the fill lands inside the baked groove (16:9; calibrate for other ratios). -->
	<era_rad_window x="5" y="734" width="83" height="10" stretch="1">
		<era_rad_fill x="10" y="4" width="70" height="3" stretch="1" horz="1" min="0" max="35" pos="0">
			<progress>
				<texture>ui_era_ihud_rad_fill</texture>
			</progress>
		</era_rad_fill>
	</era_rad_window>
</window>
